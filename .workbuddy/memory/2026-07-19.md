# 2026-07-19 代码审查

对用户安监系统做结构/死代码/逻辑审查，真实源码位于 `C:/Users/asus/Desktop/安监系统重构/`（WorkBuddy 会话目录仅含遗留 refactor 脚本）。

## 关键发现
- 🔴 `index.html:1594` 调用 `window.switchModule(currentModule)` 但该函数从未定义（真实函数为 utils.js 的 `switchTab`）。因 `typeof` 守卫不报错，但首屏无法恢复上次模块。应改为 `window.switchTab(currentModule)`。
- 🟡 死文件 `src/js/modules/issue_part1.js`：与 issue.js 完全相同的 IIFE（同 DB_NAME/STORE），全仓零引用，误加载会与 issue.js 冲突。建议删除。
- 🟡 死文件 `src/js/workers/searchWorker.js`：从未 `new Worker` 实例化（issue.js 在主线程用 Fuse 搜索）。建议删除或接入。
- 🟡 `smart-writer.js:2895-2897`：重复桩 `window.toggleDoubaoMode/saveApiConfigFromModal`（doubao.js 已定义，且 smart-writer 先于 doubao 加载会写 warn 桩再被覆盖）。冗余，建议删除两行。
- 🟢 `swipe.js:21` `_showSwipeToast` 导出未被使用（utils.js:89 注释调用）。swipe 手势本身已接线生效。
- 🟢 `app.js:28-60` 头部注释列出 engine.js/pwa.js/anti-kimi.js 三个不存在的文件（功能已内联进 app.js）。文档过时，建议更新。
- ℹ️ Fuse.js 由 issue.js:411 按需 `loadScript` 动态加载（非 index.html 静态加载），非缺陷。
- ℹ️ `issue.js updateStorage` 三段近乎相同的存储条渲染逻辑可抽公共函数。

## 结论
系统主体健康、各模块均已接线；问题集中在少量遗留死代码 + 1 个首屏模块恢复隐患 + 过时文档注释。用户尚未要求修改，仅做审查汇报。

## 批次1 已执行（21:49 起，已完成并推送）
- 删除死文件 `src/js/modules/issue_part1.js`、`src/js/workers/searchWorker.js`（全仓零引用）。
- `index.html:1594` `window.switchModule` → `window.switchTab`（修复首屏不恢复上次模块）。
- `smart-writer.js` 删除 2 行冗余桩（toggleDoubaoMode/saveApiConfigFromModal，doubao.js 已定义）。
- `issue.js updateStorage` 抽公共函数 `issueRenderStorage(sizeMB, displayQuotaMB)`，消除三段重复（~30 行→~12 行）。
- `app.js` 头部注释更新为真实模块清单（含 errorMonitor/perfMonitor/doubao-common/smart-check/smart-writer/agent-*），删除不存在的 engine.js/pwa.js/anti-kimi.js 条目。
- `node --check` 全 20 个 JS 文件全部通过。
- 提升 SW 缓存版本 `20260719`→`20260720`，确保更新生效。
- 提交: be996d8（清理）、fb9ce8a（SW 版本），已 `git push origin main`（SSH）。
- 备注：batch1 未删 `swipe.js` 的 `_showSwipeToast` 死导出（低风险，留待后续）；未碰 externals 依赖注释。

## 首屏恢复模块 - 二次修复（22:02）
- 用户反馈 batch1 后首屏仍停在「检查信息」，未恢复上次模块。
- 根因：`switchTab` 本身**从不写** `current_module`（全仓只有 index.html:1593 读取，grep 无任何 `setItem('current_module'`）。batch1 仅把 `switchModule`→`switchTab`，但读取到的永远是 null→默认 'issue'。
- 修复：`switchTab(tab, fromSwipe)` 开头加 `try{ localStorage.setItem('current_module', tab); }catch(e){}`（点击/侧滑都经此函数，一处覆盖）。
- index.html 的 initPage 仍保留 `switchTab(currentModule)` 调用作为恢复入口。
- SW 版本 `20260720`→`20260721`。提交 dbb6f3a 已 push。

## 第二梯队改造风险评估 + 执行（22:24）
用户要求"按建议执行"③⑤。逐项核查后发现与之前建议不符：
- **③ initPage dead call 已修**：`updateModeStatus` 定义在 doubao.js IIFE 内、未挂 window，导致 index.html initPage 的 `typeof updateModeStatus==='function'` 恒 false（死调用）。已在 doubao.js:302 后加 `window.updateModeStatus = updateModeStatus;`，首屏角色/模式状态标签可刷新。提交 56a3143，SW→20260722。
- **④ 搜索统一**：searchWorker.js 已在批次1删除；Fuse(issue/rule) 与 LightBM25(doubao RAG) 职责本就分离，无重复。已达标，不排期。
- **⑤ 重大修正**：原计划"diary 先改 safeHtml"前提不成立。核查确认：diary/handbook 用户数据已全量 `escapeHtml`/`_esc`；渲染模板大量依赖内联 `onclick`（复制/删除/编辑），DOMPurify 会剥掉→按钮失效；AI 响应已 `dsEsc`/`dsMarkdown` 转义，对规结论(doubao.js:1560)已用 safeHtml。**整套应用已 XSS 硬化，无裸渲染未转义不可信内容点**。故 ⑤ 不做破坏性硬转。
- 经验：改造建议前必须核实"是否真的未转义"，否则 safeHtml 包裹含内联处理器的模板会破坏功能。

## 检查更新功能修复（22:39）
- 用户问"检查更新是否起作用"。核查结论：原 `UPDATE_CHECK_URL` 指向 GitHub Releases API (`api.github.com/.../releases/latest`)，但站点纯 git push 到 GitHub Pages、从未打 Release → 点击永远返回 404 → 永远显示"暂无发布版本"，实际失效。
- `version.json`（v2.5）已存在且随 push 更新，但之前无运行时代码读取（仅 sw.js 预缓存）。`APP_VERSION` 硬编码于 app.js:490。
- 修复：app.js:491 `UPDATE_CHECK_URL` 改为 `https://haibing321.github.io/36075739-2/version.json`（SW 策略5=网络优先，不读缓存，能拿最新值）；`performUpdateCheck` 已有 `data.version` 解析分支天然兼容；404 提示改为"未找到版本信息文件"。
- 保持 `APP_VERSION` 硬编码作为运行 app.js 版本基准（比对基准），发版需同步 version.json + APP_VERSION + about 面板三者。
- SW 版本 `20260722`→`20260723`。提交 38cbe66 已 push。

## 工程化补强 + 长期记忆/清除缓存修复（22:57）
- **pre-push 钩子**：新建 `.git/hooks/pre-push`（本地、不被 git 跟踪！）+ `scripts/bump-sw.js`。钩子对全部跟踪 JS 跑 `node --check` 门禁（不通过拦截 push）；并调用 bump-sw.js 单调提升 `sw.js` 的 CACHE_VERSION（取 max(今天YYYYMMDD, 当前+1)，用 node 读写保留 CRLF）。守卫：仅当本次推送含 sw.js 之外的改动才 bump，避免"只 bump 自身"死循环（已验证：第2次仅 sw.js 时打印"跳过重复 bump"）。
- **长期记忆持久化**：doubao.js 读取/保存 `localStorage['memory_enabled']`（1/0），启动恢复勾选状态；新增 `window.clearLongTermMemory` 清空 `assistant_memory_v1`。index.html 长期记忆行加"清空"按钮。
- **清除缓存竞态修复**：app.js `clearAllCache` 改为 `Promise.all([caches.delete..., sw.unregister...]).then(reload)`，不再用固定 300ms 强刷。
- 提交 ddbd181（主批次）+ aaed8e7（SW bump 20260724→20260725）已 push。钩子自测通过。
- ⚠️ 注意：`.git/hooks/pre-push` 不被 git 跟踪，换机器/克隆后需手动复制；bump-sw.js 已被跟踪。

## 自主模式功能审查（23:03，仅分析未改代码）
用户要求检查「智能助手·自主模式」功能及不足。通读 agent-core.js / agent-memory.js / app.js 桥接 / doubao.js 入口 / diary.js / smart-writer.js / phone.js。架构、6工具、ReAct循环、所有桥接函数、IndexedDB写入均正常。发现：
- 🔴 **严重 Bug**：`agent-memory.js:41-48` 历史清理逻辑 `cursor.delete(); cursor.continue();` 无 break，会删光全部记录（而非只删最旧超量条）。第30条及之后每次保存，历史清到只剩1条 + 不可逆丢失。应改为按 overflow 计数只删最旧 (total-29) 条。
- 🔴 **功能缺口**：`getAgentTasks` 仅供 `getRecentAgentContext` 注入 AI 提示词，UI 无"查看历史"入口，用户看不到 agent 做过什么。
- 🟡 `write_diary` 参数错位：`_agentWriteDiary(content, issues)` → `addIssueToDiary(content, issues)`，而后者签名 `(content, regulation, date)`，issues 被误存进"规章依据"字段。
- 🟡 `get_weather` 依赖手动点过"查看天气"才有经纬度（phone.js:322-323 内存写入），初始数据无坐标 → agent 查多数车站返回"车站无坐标"。
- 🟡 `_parseToolCall` 正则不支持嵌套 JSON params，复杂工具调用会静默失败。
- 🟡 `maxLoops=6` 无重复调用去重、不可扩展。
- 🟢 无停止按钮 / 最终回答不渲染 Markdown / API 错误不细分 / WMO 码不全 / 写操作无确认 / 计划文本未单独渲染。
- 已生成报告 `C:/Users/asus/Desktop/安监系统重构/自主模式功能审查与改进建议.md`，按批次A(修Bug)#1#2#3、批次B#4-#9、批次C#10-#12 排列。用户尚未要求改动。

## 自主模式批次 A→C 改造（已完成并推送）
用户要求"按批次ABC顺序修改"。逐项实施 + 验证：

### 已应用（单文件改动，已 commit/push）
- **A#1 删光 Bug**：`agent-memory.js:37-54` 历史清理改为 `overflow=total-(MAX-1)` 计数，游标仅删最旧 overflow 条即停（`deleted<overflow` 守卫）。验证：35条→删6条留29条（旧逻辑会删光到1条）。严重数据安全 bug 已修。
- **A#3 write_diary 参数错位**：`app.js:149 _agentWriteDiary(content, issues)` 改为把 `issues` 并入正文（`发现问题：xxx`），调 `addIssueToDiary(fullContent, '')`，不再误存进"规章依据"字段。
- **B#4 天气坐标预取**：`phone.js:242` 新增 `window.phoneGeocode(stationName, lineName)`（复用已有 Open-Meteo Geocoding + 方位裁剪 + 甘肃/宁夏→陕西/四川两级省份过滤，不碰 DOM）；`agent-core.js get_weather` 内存无坐标时自动 `phoneGeocode` 反查，使 agent 查天气开箱即用（不再依赖手动点过天气）。
- **B#5 嵌套 JSON**：`agent-core.js:112 _parseToolCall` 改为优先解析 ```` ```json ```` 代码块，否则括号配对从首个 `{` 匹配到对应 `}`（支持 params 内嵌套对象/数组）。
- **B#6 去重/上限**：`agent-core.js:205` `maxLoops=6`→`8`；新增 `lastCallKey/repeatCount`，连续两次相同调用即提前终止（避免无效循环浪费 token）。
- **B#9 错误细分 + 密钥预检**：`_callLLM` 细分 401/429/400/网络错误并取 API 返回的 error.message；用 `AbortController`（`window.__agentAbort`）支持中断；`_agentRun` 开头预检 `ds_api_key_v1` 缺失返回友好提示，避免白跑 ReAct。
- **C#10 WMO 天气码**：agent-core get_weather 的 wmo 表补全 48/53/55/56/57/66/67/77/80/81/82/85/86/96/99，与 phone.js 对齐。
- **C#12 计划渲染**：`_agentRun` 首次工具调用前把 AI 前置说明文字单独渲染为 `📋 计划` 块。

### doubao.js 新增函数（A#2/B#7/B#8）
- `dsAgentSend`：运行态切换「⏹ 停止 / 🚀 执行」按钮可见性；最终回答从 `replace(/</g,'&lt;')` 改为 `dsMarkdown(m.content)` 渲染（与普通对话一致，含代码块下载按钮）。
- `dsAgentStop()`：`_agentRunning=false` + `window.__agentAbort.abort()` 中断在途请求，UI 标注"已手动停止"。
- `dsAgentShowHistory()`：读 `getAgentTasks(20)` 渲染历史任务（意图/步骤✅❌/时间）+「🗑 清空」按钮（**修复任务记忆只写不读**）。
- `dsAgentClearHistory()`：确认后 `indexedDB 清 agent_tasks` store。

### index.html
- 自主模式面板（643-657）：新增「📜 历史」按钮 + `ds-agent-history` 容器（A#2）；agent 输入行加 `ds-agent-stop`(隐藏)/`ds-agent-run` 按钮（B#7）。

### C#11 主动跳过（说明）
原计划"写操作二次确认/预览"在自主模式**不宜做**：write_diary/save_report 由 AI 程序化调用，`confirm()` 会阻塞 ReAct 循环致 agent 卡死。UI 已显示 🔧 写入结果步骤，可观测性足够，故跳过。

### 提交与推送
- 主批次 `1556f67`（6文件，+216/-34），pre-push 钩子语法门禁通过、SW bump 20260726。
- 报告 + SW bump `637b46d`；遗留 sw.js bump `d756e53`（SW→20260727）。
- 期间发现并修复 **pre-push 守卫 bug**：原用 `git diff origin/main...HEAD`（三点，对比分叉点）导致每次 push 都含历史业务提交 → 误判"有业务改动"而重复 bump。改为 `origin/main..HEAD`（两点，仅本地领先部分）后，纯 sw.js 推送正确"跳过重复 bump"。
- `node --check` 全部 JS 通过；A#1 删光逻辑已用 node 模拟验证。
