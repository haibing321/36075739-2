# 智能助手 · 智能体 功能审查与改进建议

> 审查日期：2026-07-19
> 审查范围：`agent-core.js`、`agent-memory.js`、`app.js`（桥接）、`doubao.js`（入口）、`diary.js`、`smart-writer.js`、`phone.js`
> 结论：**架构完整、6 个工具全部接线可用、ReAct 循环能跑通**，但存在 **1 个严重数据丢失 Bug + 1 个功能可见性缺口 + 多处正确性/体验不足**。

---

## 一、已确认可正常工作的部分 ✅

| 项 | 位置 | 说明 |
|---|---|---|
| 规划器提示词 + ReAct 循环 | `agent-core.js:131-188` | 意图分析 → 工具调用 → 最终回答，逻辑闭环 |
| 6 个工具注册 | `agent-core.js:7-80` | search_issues / search_rules / write_diary / save_report / get_weather / search_handbook |
| 桥接函数齐全 | `app.js:111-173` | `_agentGetIssues/_agentGetRules/_agentWriteDiary/_agentSaveReport/_agentGetHandbook` 均实现 |
| 底层依赖存在 | diary/smart-writer/phone | `addIssueToDiary`、`wrAgentSaveMaterial`、`getPhoneData` 均真实定义 |
| 入口与防重入 | `doubao.js:2126-2162` | `dsAgentSend` 有 `_agentRunning` 锁、模块未加载提示、错误展示 |
| 任务写入 IndexedDB | `agent-memory.js` | `saveAgentTask` 在 `_agentRun` 末尾 await 写入，正常 |

---

## 二、🔴 严重问题（必须修）

### 1. `agent-memory.js:41-48` 历史清理会**删光全部记录**（数据丢失 Bug）

```js
if (total >= 30) {
  var cursorReq = store.index('timestamp').openCursor();
  cursorReq.onsuccess = function(e2) {
    var cursor = e2.target.result;
    if (cursor) { cursor.delete(); cursor.continue(); }   // ← 没有 break
  };
}
```

游标从最旧记录开始遍历，`delete()` 后 `continue()` 会**一直走到游标结束**，删掉**所有**记录——而非只删最旧的超量部分。

**后果**：当记录数 ≥ 30 时，每次保存新任务都会把历史清空到只剩刚插入的 1 条。所谓"保留最近 30 条"完全失效，且是**不可逆数据丢失**。

**修复（只删最旧的超量条）**：
```js
if (total >= 30) {
  var overflow = total - 29;          // 需删除的条数
  var cursorReq = store.index('timestamp').openCursor(); // 默认升序=最旧在前
  cursorReq.onsuccess = function(e2) {
    var cursor = e2.target.result;
    if (cursor && overflow > 0) { cursor.delete(); overflow--; cursor.continue(); }
  };
}
```

### 2. 任务记忆"只写不读"——UI 无任何历史查看入口

`getAgentTasks`（`agent-memory.js:57`）仅在 `getRecentAgentContext`（:82）内部被调用，用途是**注入给 AI 的提示词**。全仓 grep 不到任何 UI 调用它来渲染列表。

**后果**：用户无法回顾"让 agent 做过什么、结果如何"——"任务记忆"功能对用户不可见、不可复盘、不可管理。

**修复**：在智能体面板加"📜 历史"按钮，调用 `getAgentTasks` 渲染最近 N 条（意图 / 步骤 / 结果 / 时间），并可加"清空历史"。

---

## 三、🟡 正确性问题

### 3. `write_diary` 参数错位：issues 被当成"规章依据"存

- 桥接：`_agentWriteDiary(content, issues)` → `addIssueToDiary(content, issues)`
- 实际签名：`addIssueToDiary(content, regulation, date)`（`diary.js:128`）

→ agent 传入的"发现的问题(issues)"被存进 `regulations`（规章依据）字段，真正的规章依据为空。**写入能成功，但语义错误、数据不准**。

**修复**：桥接改为 `addIssueToDiary(content + (issues ? '\n' + issues : ''), '', '')`，或扩展 `addIssueToDiary` 支持 issues 参数。

### 4. `get_weather` 依赖"手动查过天气"才有坐标

工具读 `phoneData[i].纬度/经度`（`agent-core.js:60,62`），但这两个字段**仅在用户点"查看天气"后**由 `phoneGetWeather` 写入内存变量（`phone.js:322-323`）。初始加载的应急电话数据**没有经纬度**。

**后果**：agent 查多数车站会返回"车站无坐标，请先手动查询"——自动查天气在"未预查过"的车站上不可用。

**修复**：智能体首次进入时，复用 `phone.js` 已有的"省份两级过滤 + 方位裁剪"Geocoding 逻辑，批量反查所有车站经纬度并缓存到 `phoneData`，使 `get_weather` 开箱即用。

### 5. `_parseToolCall` 不支持嵌套 JSON params

正则 `\{[^{}]*"tool"...\}` 仅匹配**无花括号嵌套**的参数。若模型返回带嵌套对象/数组的 params（如 `save_report` 的 `content` 含结构），解析失败 → 该轮被当作"最终回答"跳过 → **工具静默不执行**。

**修复**：优先解析 ```` ```json ... ``` ```` 代码块；否则用"括号配对"方式从第一个 `{` 匹配到对应的 `}`。

### 6. `maxLoops=6` 无去重、不可扩展

- 没有对"连续两轮调用同一工具且结果相同"的去重，模型若陷入重复会白白消耗 6 轮 token；
- 6 轮是硬上限，复杂任务（如"对比 3 个站天气再生成报告"）可能不够。

**修复**：加"连续重复工具调用检测"提前终止；`maxLoops` 根据意图复杂度提到 8–10。

---

## 四、🟢 体验 / 工程不足

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| 7 | 无"停止/中断"按钮 | `doubao.js` 入口 | 最长卡 6 轮 API，用户只能干等 |
| 8 | 最终回答不渲染 Markdown | `doubao.js:2150` | 直接 `replace(/</g,'&lt;')` 显示，标题/列表/代码块平铺，与普通对话 `dsMarkdown` 不一致 |
| 9 | API 错误不细分 | `agent-core.js:125` | 401 密钥错 / 429 限流 / 超时 混为"执行错误"；且无密钥预检查 |
| 10 | 天气 WMO 码映射不全 | `agent-core.js:64` | 缺 48/53-57/80-86 等，与 `phone.js` 映射可能不一致 |
| 11 | 写操作无二次确认/撤回 | `write_diary`/`save_report` | AI 误解意图可能写入错误日志/报告 |
| 12 | 计划文本未单独渲染 | `agent-core.js:158` | `agent-plan` 只显示静态"🤖 智能体·启动"，模型说的计划不可见 |

---

## 五、改进建议（按优先级）

| 优先级 | 项 | 文件 | 动作 |
|---|---|---|---|
| 🔴 高 | #1 删光 Bug | `agent-memory.js` | 改为只删最旧超量条（见上） |
| 🔴 高 | #2 历史可见 | `index.html` + `doubao.js` | 加"📜 历史"按钮渲染 `getAgentTasks` + 清空入口 |
| 🔴 高 | #3 参数错位 | `app.js:148` | 修正 `addIssueToDiary` 调用语义 |
| 🟡 中 | #4 天气坐标 | `agent-core.js` + `phone.js` | 进入智能体时批量 Geocoding 反查并经纬度缓存 |
| 🟡 中 | #5 JSON 解析 | `agent-core.js:104` | 增强 `_parseToolCall` 支持嵌套/代码块 |
| 🟡 中 | #6 循环控制 | `agent-core.js:159` | 加重复检测 + 提升 maxLoops |
| 🟢 低 | #7 停止按钮 | `doubao.js` | AbortController 中断当前 fetch |
| 🟢 低 | #8 Markdown | `doubao.js:2150` | 复用 `dsMarkdown` 渲染 |
| 🟢 低 | #9 错误处理 | `agent-core.js` | 细分 HTTP 状态 + 启动预检密钥 |
| 🟢 低 | #10 WMO | `agent-core.js` | 补全天气码，与 phone.js 对齐 |
| 🟢 低 | #11 写确认 | `agent-core.js` | 高风险写操作加确认/预览 |
| 🟢 低 | #12 计划渲染 | `agent-core.js` | 单独渲染模型计划区块 |

---

## 六、推荐执行顺序

```
批次 A（修 Bug，必做，低风险）：#1 + #2 + #3
批次 B（正确性/体验，中风险）：#4 + #5 + #6 + #7 + #8 + #9
批次 C（增强，可选）：#10 + #11 + #12
```

> 注：批次 A 中 #1 是数据安全硬 bug，建议**优先单独修并尽快推送**，避免历史任务继续被悄悄清空。
